import 'package:flutter/material.dart';

class GraphicalHomeView extends StatelessWidget {
  const GraphicalHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Graphical View'),
    );
  }
}
