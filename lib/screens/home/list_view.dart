import 'package:flutter/material.dart';

class ListHomeView extends StatelessWidget {
  const ListHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('ListView'),
    );
  }
}
